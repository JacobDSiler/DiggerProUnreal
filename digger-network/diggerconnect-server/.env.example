# Port is auto-provided on Render; locally we'll use 3000
PORT=3000

# Comma-separated list of allowed origins for CORS. Use "*" during local testing.
ALLOWED_ORIGIN=*

# Reserved for later (e.g., public key for offline license checks)
LICENSE_PUBLIC_KEY=
